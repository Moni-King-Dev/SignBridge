# SignBridge (Full Project)

This repository contains:
- backend/ — Flask + TensorFlow model for ISL prediction
- convex/ — Convex stubs (optional)
- src/ — React + Vite + Tailwind frontend
- public/videos/ — (place Blender-rendered .mp4 videos here)

## Quickstart

### Backend
1. Create a Python venv:
   ```bash
   cd backend
   python -m venv venv
   # Windows:
   venv\Scripts\activate
   # macOS/Linux:
   source venv/bin/activate
